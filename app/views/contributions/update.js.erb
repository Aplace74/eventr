console.log('salut')
function refreshUpdateForm(innerHTML) {
  const supplyCard = document.querySelector("[data-supply-id='<%= @supply.id %>']");
  supplyCard.innerHTML = innerHTML;
}

refreshUpdateForm('<%= j render "supply_card", supply: @supply, rest: @supply.rest, participation: @participation %>')

bindEventListenersToCards()