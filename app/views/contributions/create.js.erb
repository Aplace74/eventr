function refreshCreateForm(innerHTML) {
  const supplyCard = document.querySelector("[data-supply-id='<%= @supply.id %>']");
  supplyCard.innerHTML = innerHTML;
}

refreshCreateForm("<%= j render 'supply_card', supply: @supply, rest: @supply.rest, participation: @participation %>")

bindEventListenersToCards()